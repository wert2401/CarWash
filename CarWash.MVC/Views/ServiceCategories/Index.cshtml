@model IEnumerable<CarWash.Database.Models.ServiceCategory>

@{
    ViewData["Title"] = "Service Categories";
}

<h1>Categories</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ServiceCategoryId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ServiceCategoryId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.ServiceCategoryId }) |
                @Html.ActionLink("Details", "Details", new { id=item.ServiceCategoryId }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.ServiceCategoryId })
            </td>
        </tr>
}
    </tbody>
</table>
